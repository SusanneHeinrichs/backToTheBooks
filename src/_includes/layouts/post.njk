{% extends "layouts/base.njk" %}

{% block content %}
    <main class="container my-5 flex-grow-1 col-lg-8 justify-content-center">
        <h1>{{ title }}</h1>
        <p><strong>von {{ author }}</strong><span style="float: right;">{{ page.date | dateTimeString }}</span></p>
        <p></p>
        {{ content|safe }}
    </main>
{% endblock content %}
